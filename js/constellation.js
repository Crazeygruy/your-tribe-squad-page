// Constellation · entry point.
// The render logic lives in ./constellation/ (index, stars, preview, render,
// utils) and the member data in ./constellation-data.js.

import { initConstellation } from "./constellation/index.js";

initConstellation();